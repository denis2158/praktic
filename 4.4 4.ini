
fn main() {
    p        1 => Some(42),
        _ => None, // тут можна викликати never_return_fn() замість None


rintln!("Success!");
    // Ця функція демонстраційно ніколи не повертає
    // let _ = get_option(2);
}

fn get_option(tp: u8) -> Option<i32> {
    match tp {
        1 => Some(42),
        _ => None, // тут можна викликати never_return_fn() замість None
    }
    // Якщо хочемо викликати diverging function:
    // never_return_fn()
}

// IMPLEMENT this function in THREE ways

// --- Way 1: паніка ---
fn never_return_fn() -> ! {
    panic!("This function never returns!")
}

// --- Way 2: нескінченний цикл ---
/*
fn never_return_fn() -> ! {
    loop {
        // нескінченний цикл, функція ніколи не повертає
    }
}
*/

// --- Way 3: виклик іншої diverging функції ---
/*
fn never_return_fn() -> ! {
    std::process::exit(1); // завершує програму, ніколи не повертає
}
*/
